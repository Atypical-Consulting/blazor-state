---
phase: 06-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Build.props
  - src/Bustand/Bustand.csproj
  - src/Bustand.DevTools/Bustand.DevTools.csproj
autonomous: true

must_haves:
  truths:
    - "dotnet pack produces valid .nupkg files for both Bustand and Bustand.DevTools"
    - "Packages target both .NET 8 and .NET 10"
    - "Package metadata includes description, tags, license, repository URL"
  artifacts:
    - path: "Directory.Build.props"
      provides: "Centralized NuGet metadata and multi-targeting"
      contains: "TargetFrameworks"
    - path: "src/Bustand/Bustand.csproj"
      provides: "Bustand package-specific metadata"
      contains: "PackageId"
    - path: "src/Bustand.DevTools/Bustand.DevTools.csproj"
      provides: "DevTools package-specific metadata"
      contains: "PackageId"
  key_links:
    - from: "Directory.Build.props"
      to: "*.csproj"
      via: "MSBuild import"
      pattern: "Version|Authors|PackageLicenseExpression"
    - from: "Bustand.DevTools.csproj"
      to: "Bustand.csproj"
      via: "Version-locked dependency"
      pattern: "Version.*\\$\\(Version\\)"
---

<objective>
Configure NuGet package metadata and multi-targeting for both Bustand and Bustand.DevTools packages.

Purpose: Enable packages to be built and published to NuGet.org with complete metadata, supporting both .NET 8 and .NET 10 consumers.
Output: Updated Directory.Build.props with shared metadata, updated csproj files with package-specific metadata.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-RESEARCH.md
@Directory.Build.props
@src/Bustand/Bustand.csproj
@src/Bustand.DevTools/Bustand.DevTools.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Directory.Build.props with centralized NuGet metadata</name>
  <files>Directory.Build.props</files>
  <action>
Update Directory.Build.props to include:

1. Multi-targeting: Change `<TargetFramework>net10.0</TargetFramework>` to `<TargetFrameworks>net8.0;net10.0</TargetFrameworks>` (note plural)

2. Add shared NuGet metadata:
   - Authors: Philippe Matray
   - Company: phmatray
   - Copyright: Copyright (c) Philippe Matray 2026
   - PackageLicenseExpression: MIT
   - PackageProjectUrl: https://github.com/phmatray/Bustand
   - RepositoryUrl: https://github.com/phmatray/Bustand.git
   - RepositoryType: git

3. Add symbol/debugging configuration:
   - DebugType: embedded
   - EmbedUntrackedSources: true
   - PublishRepositoryUrl: true

4. Add documentation generation:
   - GenerateDocumentationFile: true

5. Add version properties:
   - Version: 0.1.0
   - PackageVersion: 0.1.0

Keep existing: LangVersion, Nullable, ImplicitUsings, TreatWarningsAsErrors

Per RESEARCH.md: Use MSBuild properties, not .nuspec files. Use TargetFrameworks (plural) for multi-targeting.
  </action>
  <verify>
Run `dotnet build` from solution root - should build successfully for both frameworks.
Check Directory.Build.props contains TargetFrameworks with both net8.0 and net10.0.
  </verify>
  <done>Directory.Build.props has complete NuGet metadata, multi-targeting, and embedded symbols configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Update Bustand.csproj with package-specific metadata</name>
  <files>src/Bustand/Bustand.csproj</files>
  <action>
Update src/Bustand/Bustand.csproj to:

1. REMOVE the local TargetFramework property (inherited from Directory.Build.props now)

2. Add package-specific PropertyGroup:
   - PackageId: Bustand
   - Description: Zustand-inspired state management for Blazor. Minimal boilerplate, immutable state with records, middleware architecture, persistence support. Works across Server, WebAssembly, and Auto rendering modes.
   - PackageTags: blazor;state-management;zustand;store;flux;wasm;blazor-server;dotnet;middleware
   - PackageReadmeFile: README.md
   - PackageIcon: icon.png
   - PackageReleaseNotes: Initial release with core store functionality, middleware architecture, persistence support, and DevTools integration.

3. Add ItemGroup for packaging README and icon (to be created in Plan 02):
   ```xml
   <ItemGroup>
     <None Include="../../README.md" Pack="true" PackagePath="" />
     <None Include="../../icon.png" Pack="true" PackagePath="" />
   </ItemGroup>
   ```

4. Keep existing: RootNamespace, InternalsVisibleTo, SupportedPlatform, FrameworkReference, PackageReferences

Per RESEARCH.md: Package-specific properties go in individual csproj, shared properties in Directory.Build.props.
  </action>
  <verify>
Run `dotnet build src/Bustand/Bustand.csproj` - should succeed (README/icon missing is warning, not error).
Grep for PackageId, Description, PackageTags in csproj.
  </verify>
  <done>Bustand.csproj has complete package-specific metadata and references README/icon for packaging.</done>
</task>

<task type="auto">
  <name>Task 3: Update Bustand.DevTools.csproj with package-specific metadata and version lock</name>
  <files>src/Bustand.DevTools/Bustand.DevTools.csproj</files>
  <action>
Update src/Bustand.DevTools/Bustand.DevTools.csproj to:

1. REMOVE local TargetFramework, Nullable, ImplicitUsings properties (inherited from Directory.Build.props now)

2. Add package-specific PropertyGroup:
   - PackageId: Bustand.DevTools
   - Description: DevTools for Bustand state management. Inspect state in real-time, view action history with timestamps, time-travel debug through state changes, and see diffs between states. Development environment only.
   - PackageTags: blazor;state-management;zustand;devtools;debugging;time-travel;state-inspector
   - PackageReadmeFile: README.md
   - PackageIcon: icon.png
   - PackageReleaseNotes: Initial release with state inspector, action history, time-travel debugging, and diff view.

3. Add ItemGroup for packaging README and icon:
   ```xml
   <ItemGroup>
     <None Include="../../README.md" Pack="true" PackagePath="" />
     <None Include="../../icon.png" Pack="true" PackagePath="" />
   </ItemGroup>
   ```

4. CRITICAL - Update ProjectReference to use version-locked PackageReference for Release builds:
   ```xml
   <!-- For development: use ProjectReference -->
   <ItemGroup Condition="'$(Configuration)' != 'Release'">
     <ProjectReference Include="..\Bustand\Bustand.csproj" />
   </ItemGroup>

   <!-- For Release/packaging: use exact version PackageReference -->
   <ItemGroup Condition="'$(Configuration)' == 'Release'">
     <PackageReference Include="Bustand" Version="[$(Version)]" />
   </ItemGroup>
   ```

5. Keep existing: RootNamespace, InternalsVisibleTo, SupportedPlatform

6. REMOVE explicit Microsoft.AspNetCore.Components.Web PackageReference (comes via FrameworkReference in Bustand)

Per RESEARCH.md: DevTools must depend on exact same version of Bustand core. Use [$(Version)] for exact match.
  </action>
  <verify>
Run `dotnet build src/Bustand.DevTools/Bustand.DevTools.csproj` - should succeed.
Grep for "Condition.*Release" and "Version.*\[" to verify version lock.
  </verify>
  <done>Bustand.DevTools.csproj has package metadata and version-locked dependency on Bustand core.</done>
</task>

</tasks>

<verification>
1. `dotnet build Bustand.sln` succeeds for both frameworks (net8.0 and net10.0)
2. `dotnet pack src/Bustand/Bustand.csproj --no-build` produces warning about missing README/icon (expected, created in Plan 02)
3. `dotnet pack src/Bustand.DevTools/Bustand.DevTools.csproj --no-build` produces warning about missing README/icon
4. Inspect Directory.Build.props - contains TargetFrameworks, Version, Authors, License
5. Inspect csproj files - contain PackageId, Description, PackageTags
</verification>

<success_criteria>
- Both packages build successfully targeting net8.0 and net10.0
- dotnet pack produces .nupkg files with correct metadata
- Version 0.1.0 is set in Directory.Build.props
- DevTools has version-locked dependency on Bustand core
- DIST-01, DIST-02, DIST-03 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-01-SUMMARY.md`
</output>
