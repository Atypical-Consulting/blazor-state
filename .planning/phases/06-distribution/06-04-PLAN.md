---
phase: 06-distribution
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - samples/Bustand.Sample/Stores/CounterStore.cs
  - samples/Bustand.Sample/Stores/CounterState.cs
  - samples/Bustand.Sample.Client/Stores/TodoStore.cs
  - samples/Bustand.Sample.Client/Stores/TodoState.cs
  - samples/Bustand.Sample.Client/Stores/ShoppingCartStore.cs
  - samples/Bustand.Sample.Client/Stores/ShoppingCartState.cs
autonomous: true

must_haves:
  truths:
    - "CounterStore demonstrates basic increment/decrement"
    - "TodoStore demonstrates list management with add/remove/toggle"
    - "ShoppingCartStore demonstrates nested objects with items and quantities"
    - "All stores use persistence via PersistAttribute"
  artifacts:
    - path: "samples/Bustand.Sample/Stores/CounterStore.cs"
      provides: "Simple counter store for Server rendering"
      contains: "ZustandStore"
    - path: "samples/Bustand.Sample.Client/Stores/TodoStore.cs"
      provides: "TodoList store for WASM/Auto rendering"
      contains: "ZustandStore"
    - path: "samples/Bustand.Sample.Client/Stores/ShoppingCartStore.cs"
      provides: "Shopping cart store with nested objects"
      contains: "ZustandStore"
  key_links:
    - from: "CounterStore"
      to: "PersistAttribute"
      via: "Attribute decoration"
      pattern: "\\[Persist\\]"
    - from: "TodoStore"
      to: "ImmutableList"
      via: "State property"
      pattern: "ImmutableList<TodoItem>"
---

<objective>
Create the three example stores (Counter, TodoList, ShoppingCart) with comprehensive comments explaining Bustand concepts.

Purpose: Provide teaching-focused store implementations that demonstrate progressively complex state management patterns.
Output: Three stores with record states, persistence, and extensive explanatory comments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-RESEARCH.md
@.planning/phases/06-distribution/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CounterStore (simple state, Server-side)</name>
  <files>
    samples/Bustand.Sample/Stores/CounterState.cs
    samples/Bustand.Sample/Stores/CounterStore.cs
  </files>
  <action>
Create Counter store in Server project (demonstrates basic patterns):

1. **Stores/CounterState.cs**:
   ```csharp
   namespace Bustand.Sample.Stores;

   /// <summary>
   /// State for the Counter store.
   ///
   /// Key concepts demonstrated:
   /// - C# record for immutable state (with expressions work automatically)
   /// - Simple value types in state
   /// - State is a plain data container - no behavior
   /// </summary>
   public record CounterState(
       /// <summary>The current count value.</summary>
       int Count
   );
   ```

2. **Stores/CounterStore.cs**:
   ```csharp
   using Bustand.Persistence;

   namespace Bustand.Sample.Stores;

   /// <summary>
   /// A simple counter store demonstrating basic Bustand patterns.
   ///
   /// Key concepts demonstrated:
   /// - Inheriting from ZustandStore&lt;TState&gt;
   /// - Defining InitialState
   /// - Using Set() with 'with' expressions for immutable updates
   /// - Persistence via [Persist] attribute
   ///
   /// This is the simplest possible store - perfect for learning the basics.
   /// </summary>
   [Persist(StorageType.LocalStorage, Key = "counter")]
   public class CounterStore : ZustandStore<CounterState>
   {
       /// <summary>
       /// Define the initial state. This is called once when the store is created.
       /// If persistence is enabled and data exists, it will be restored after this.
       /// </summary>
       public override CounterState InitialState => new(Count: 0);

       /// <summary>
       /// Increment the counter by 1.
       ///
       /// Notice: We use Set() with a lambda that receives the current state.
       /// The 'with' expression creates a new record with the updated value.
       /// This pattern ensures immutability - we never modify state directly.
       /// </summary>
       public void Increment()
       {
           // Set() accepts a function: currentState => newState
           // The 'with' expression creates a copy with modified properties
           Set(state => state with { Count = state.Count + 1 });
       }

       /// <summary>
       /// Decrement the counter by 1.
       /// </summary>
       public void Decrement()
       {
           Set(state => state with { Count = state.Count - 1 });
       }

       /// <summary>
       /// Reset the counter to zero.
       ///
       /// Alternative pattern: Set() also accepts a direct state value.
       /// Use this when you don't need the previous state.
       /// </summary>
       public void Reset()
       {
           // Direct value instead of function - useful for complete reset
           Set(new CounterState(Count: 0));
       }

       /// <summary>
       /// Increment by a specific amount.
       /// Shows how to pass parameters to store methods.
       /// </summary>
       public void IncrementBy(int amount)
       {
           Set(state => state with { Count = state.Count + amount });
       }
   }
   ```

Per CONTEXT.md: Counter demonstrates basic state. Extensive comments explain every pattern.
  </action>
  <verify>
Files exist at samples/Bustand.Sample/Stores/CounterState.cs and CounterStore.cs.
dotnet build samples/Bustand.Sample/Bustand.Sample.csproj succeeds.
  </verify>
  <done>CounterStore created with full documentation comments explaining basic Bustand patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Create TodoStore (list management, Client-side)</name>
  <files>
    samples/Bustand.Sample.Client/Stores/TodoState.cs
    samples/Bustand.Sample.Client/Stores/TodoStore.cs
  </files>
  <action>
Create Todo store in Client project (demonstrates list management):

1. **Stores/TodoState.cs**:
   ```csharp
   using System.Collections.Immutable;

   namespace Bustand.Sample.Client.Stores;

   /// <summary>
   /// A single todo item.
   ///
   /// Key concepts demonstrated:
   /// - Nested records in state
   /// - Using Guid for unique identification
   /// </summary>
   public record TodoItem(
       Guid Id,
       string Text,
       bool IsCompleted
   );

   /// <summary>
   /// State for the TodoList store.
   ///
   /// Key concepts demonstrated:
   /// - Using ImmutableList for collections (from System.Collections.Immutable)
   /// - Collections in state should always be immutable
   /// - Filter property shows derived/computed state
   /// </summary>
   public record TodoState(
       /// <summary>All todo items.</summary>
       ImmutableList<TodoItem> Items,
       /// <summary>Current filter (All, Active, Completed).</summary>
       TodoFilter Filter
   );

   /// <summary>
   /// Filter options for displaying todos.
   /// </summary>
   public enum TodoFilter
   {
       All,
       Active,
       Completed
   }
   ```

2. **Stores/TodoStore.cs**:
   ```csharp
   using System.Collections.Immutable;
   using Bustand.Persistence;

   namespace Bustand.Sample.Client.Stores;

   /// <summary>
   /// A TodoList store demonstrating list management patterns.
   ///
   /// Key concepts demonstrated:
   /// - Working with ImmutableList (Add, Remove, Replace)
   /// - Finding and updating items in a list
   /// - Derived state (computed properties)
   /// - More complex state mutations
   ///
   /// This store lives in the Client project because TodoList pages
   /// use InteractiveWebAssembly and InteractiveAuto render modes.
   /// WASM-capable components must be in the Client project.
   /// </summary>
   [Persist(StorageType.LocalStorage, Key = "todos")]
   public class TodoStore : ZustandStore<TodoState>
   {
       /// <summary>
       /// Initialize with empty list and "All" filter.
       /// </summary>
       public override TodoState InitialState => new(
           Items: ImmutableList<TodoItem>.Empty,
           Filter: TodoFilter.All
       );

       // ========================================
       // Derived/Computed State
       // ========================================
       // These properties compute values from state.
       // They're recalculated on each access - no caching.
       // For expensive computations, consider memoization.

       /// <summary>
       /// Get items filtered by current filter setting.
       /// This is derived state - computed from the actual state.
       /// </summary>
       public IEnumerable<TodoItem> FilteredItems => State.Filter switch
       {
           TodoFilter.Active => State.Items.Where(i => !i.IsCompleted),
           TodoFilter.Completed => State.Items.Where(i => i.IsCompleted),
           _ => State.Items
       };

       /// <summary>
       /// Count of items not yet completed.
       /// </summary>
       public int ActiveCount => State.Items.Count(i => !i.IsCompleted);

       /// <summary>
       /// Whether all items are completed.
       /// </summary>
       public bool AllCompleted => State.Items.Count > 0 && State.Items.All(i => i.IsCompleted);

       // ========================================
       // State Mutations
       // ========================================

       /// <summary>
       /// Add a new todo item.
       ///
       /// ImmutableList.Add() returns a NEW list with the item added.
       /// The original list is unchanged - that's immutability.
       /// </summary>
       public void AddTodo(string text)
       {
           if (string.IsNullOrWhiteSpace(text)) return;

           var newItem = new TodoItem(
               Id: Guid.NewGuid(),
               Text: text.Trim(),
               IsCompleted: false
           );

           Set(state => state with
           {
               Items = state.Items.Add(newItem)
           });
       }

       /// <summary>
       /// Remove a todo item by ID.
       ///
       /// ImmutableList.RemoveAll() returns a new list without matching items.
       /// </summary>
       public void RemoveTodo(Guid id)
       {
           Set(state => state with
           {
               Items = state.Items.RemoveAll(i => i.Id == id)
           });
       }

       /// <summary>
       /// Toggle the completed status of a todo item.
       ///
       /// Pattern for updating an item in a list:
       /// 1. Find the item
       /// 2. Create updated version with 'with'
       /// 3. Replace in list using ImmutableList.Replace()
       /// </summary>
       public void ToggleTodo(Guid id)
       {
           Set(state =>
           {
               var item = state.Items.FirstOrDefault(i => i.Id == id);
               if (item is null) return state;

               var updatedItem = item with { IsCompleted = !item.IsCompleted };
               return state with
               {
                   Items = state.Items.Replace(item, updatedItem)
               };
           });
       }

       /// <summary>
       /// Update the text of a todo item.
       /// </summary>
       public void UpdateTodoText(Guid id, string newText)
       {
           if (string.IsNullOrWhiteSpace(newText)) return;

           Set(state =>
           {
               var item = state.Items.FirstOrDefault(i => i.Id == id);
               if (item is null) return state;

               var updatedItem = item with { Text = newText.Trim() };
               return state with
               {
                   Items = state.Items.Replace(item, updatedItem)
               };
           });
       }

       /// <summary>
       /// Mark all items as completed.
       ///
       /// ImmutableList.Select() creates a new list with transformed items.
       /// ToImmutableList() converts back to ImmutableList.
       /// </summary>
       public void CompleteAll()
       {
           Set(state => state with
           {
               Items = state.Items
                   .Select(i => i with { IsCompleted = true })
                   .ToImmutableList()
           });
       }

       /// <summary>
       /// Remove all completed items.
       /// </summary>
       public void ClearCompleted()
       {
           Set(state => state with
           {
               Items = state.Items.RemoveAll(i => i.IsCompleted)
           });
       }

       /// <summary>
       /// Change the filter setting.
       /// </summary>
       public void SetFilter(TodoFilter filter)
       {
           Set(state => state with { Filter = filter });
       }
   }
   ```

Per CONTEXT.md: TodoList shows list management. Located in Client project for WASM/Auto support.
  </action>
  <verify>
Files exist at samples/Bustand.Sample.Client/Stores/TodoState.cs and TodoStore.cs.
dotnet build samples/Bustand.Sample.Client/Bustand.Sample.Client.csproj succeeds.
  </verify>
  <done>TodoStore created with list management patterns and comprehensive documentation.</done>
</task>

<task type="auto">
  <name>Task 3: Create ShoppingCartStore (nested objects, Client-side)</name>
  <files>
    samples/Bustand.Sample.Client/Stores/ShoppingCartState.cs
    samples/Bustand.Sample.Client/Stores/ShoppingCartStore.cs
  </files>
  <action>
Create ShoppingCart store in Client project (demonstrates nested objects):

1. **Stores/ShoppingCartState.cs**:
   ```csharp
   using System.Collections.Immutable;

   namespace Bustand.Sample.Client.Stores;

   /// <summary>
   /// A product that can be added to the cart.
   /// In a real app, this might come from an API.
   /// </summary>
   public record Product(
       string Id,
       string Name,
       decimal Price,
       string Category
   );

   /// <summary>
   /// An item in the shopping cart (product + quantity).
   ///
   /// Key concepts demonstrated:
   /// - Nested record containing another record (Product)
   /// - Computed property (Total) derived from other properties
   /// </summary>
   public record CartItem(
       Product Product,
       int Quantity
   )
   {
       /// <summary>
       /// Computed total for this line item.
       /// </summary>
       public decimal Total => Product.Price * Quantity;
   }

   /// <summary>
   /// State for the ShoppingCart store.
   ///
   /// Key concepts demonstrated:
   /// - Deeply nested state (State > CartItem > Product)
   /// - ImmutableDictionary for keyed collections
   /// - Multiple computed properties
   /// </summary>
   public record ShoppingCartState(
       /// <summary>
       /// Cart items keyed by product ID for fast lookup.
       /// Using ImmutableDictionary instead of ImmutableList when you need key-based access.
       /// </summary>
       ImmutableDictionary<string, CartItem> Items,

       /// <summary>
       /// Whether checkout is in progress.
       /// </summary>
       bool IsCheckingOut
   );
   ```

2. **Stores/ShoppingCartStore.cs**:
   ```csharp
   using System.Collections.Immutable;
   using Bustand.Persistence;

   namespace Bustand.Sample.Client.Stores;

   /// <summary>
   /// A shopping cart store demonstrating advanced patterns with nested objects.
   ///
   /// Key concepts demonstrated:
   /// - Working with ImmutableDictionary
   /// - Deeply nested state updates
   /// - Async operations (SetAsync for checkout)
   /// - More complex derived state calculations
   /// - Multiple computed properties
   ///
   /// This is the most advanced example - study after understanding Counter and TodoList.
   /// </summary>
   [Persist(StorageType.SessionStorage, Key = "cart")]  // Session storage - cart clears when browser closes
   public class ShoppingCartStore : ZustandStore<ShoppingCartState>
   {
       /// <summary>
       /// Initialize with empty cart.
       /// </summary>
       public override ShoppingCartState InitialState => new(
           Items: ImmutableDictionary<string, CartItem>.Empty,
           IsCheckingOut: false
       );

       // ========================================
       // Derived/Computed State
       // ========================================

       /// <summary>
       /// All items in the cart as a list (for iteration).
       /// </summary>
       public IEnumerable<CartItem> Items => State.Items.Values;

       /// <summary>
       /// Total number of items (sum of quantities).
       /// </summary>
       public int TotalItemCount => State.Items.Values.Sum(i => i.Quantity);

       /// <summary>
       /// Total number of unique products.
       /// </summary>
       public int UniqueItemCount => State.Items.Count;

       /// <summary>
       /// Total price of all items.
       /// </summary>
       public decimal TotalPrice => State.Items.Values.Sum(i => i.Total);

       /// <summary>
       /// Whether the cart is empty.
       /// </summary>
       public bool IsEmpty => State.Items.Count == 0;

       // ========================================
       // State Mutations
       // ========================================

       /// <summary>
       /// Add a product to the cart or increase its quantity.
       ///
       /// Pattern for dictionary operations:
       /// - Use TryGetValue to check existence
       /// - SetItem adds or replaces a key-value pair
       /// </summary>
       public void AddToCart(Product product, int quantity = 1)
       {
           if (quantity <= 0) return;

           Set(state =>
           {
               if (state.Items.TryGetValue(product.Id, out var existing))
               {
                   // Product already in cart - increase quantity
                   var updated = existing with { Quantity = existing.Quantity + quantity };
                   return state with { Items = state.Items.SetItem(product.Id, updated) };
               }
               else
               {
                   // New product - add to cart
                   var newItem = new CartItem(product, quantity);
                   return state with { Items = state.Items.Add(product.Id, newItem) };
               }
           });
       }

       /// <summary>
       /// Remove a product from the cart entirely.
       /// </summary>
       public void RemoveFromCart(string productId)
       {
           Set(state => state with
           {
               Items = state.Items.Remove(productId)
           });
       }

       /// <summary>
       /// Update the quantity of a product in the cart.
       /// Removes the item if quantity becomes 0 or less.
       /// </summary>
       public void UpdateQuantity(string productId, int newQuantity)
       {
           Set(state =>
           {
               if (!state.Items.TryGetValue(productId, out var existing))
                   return state;

               if (newQuantity <= 0)
               {
                   // Remove item if quantity is 0 or negative
                   return state with { Items = state.Items.Remove(productId) };
               }

               var updated = existing with { Quantity = newQuantity };
               return state with { Items = state.Items.SetItem(productId, updated) };
           });
       }

       /// <summary>
       /// Clear all items from the cart.
       /// </summary>
       public void ClearCart()
       {
           Set(state => state with
           {
               Items = ImmutableDictionary<string, CartItem>.Empty
           });
       }

       /// <summary>
       /// Simulate checkout process.
       ///
       /// Demonstrates SetAsync for async operations:
       /// - Set loading state before async work
       /// - Perform async operation
       /// - Update state after completion
       ///
       /// In a real app, this would call a payment API.
       /// </summary>
       public async Task CheckoutAsync()
       {
           // Set checking out flag
           Set(state => state with { IsCheckingOut = true });

           try
           {
               // Simulate API call
               await Task.Delay(1500);

               // Clear cart after successful checkout
               Set(state => state with
               {
                   Items = ImmutableDictionary<string, CartItem>.Empty,
                   IsCheckingOut = false
               });
           }
           catch
           {
               // Reset flag on error
               Set(state => state with { IsCheckingOut = false });
               throw;
           }
       }
   }
   ```

Per CONTEXT.md: ShoppingCart shows nested objects. Uses SessionStorage (clears on browser close).
  </action>
  <verify>
Files exist at samples/Bustand.Sample.Client/Stores/ShoppingCartState.cs and ShoppingCartStore.cs.
dotnet build samples/Bustand.Sample.Client/Bustand.Sample.Client.csproj succeeds.
  </verify>
  <done>ShoppingCartStore created with nested object patterns and comprehensive documentation.</done>
</task>

</tasks>

<verification>
1. `dotnet build Bustand.sln` succeeds
2. All three stores have [Persist] attribute
3. CounterStore in Server project, TodoStore and ShoppingCartStore in Client project
4. All state records use immutable collections (ImmutableList, ImmutableDictionary)
5. Extensive XML documentation comments on all public members
</verification>

<success_criteria>
- Three stores demonstrate progressively complex patterns
- All stores persist (Counter + Todo to LocalStorage, Cart to SessionStorage)
- Stores have tutorial-quality comments explaining every pattern
- Counter in Server project, Todo and ShoppingCart in Client project (WASM-capable)
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-04-SUMMARY.md`
</output>
